# Determine whether Python has a method to lowercase a string, for example converting 'Aloha, World!' into 'aloha, world!.

lowercase = 'Aloha, World!'.lower()
print(lowercase)

# str.lower() => returns a copy of the string with all the cases characters converted to lowercase.
